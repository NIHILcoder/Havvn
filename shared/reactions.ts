/**
 * Chat-message reaction emoji — ONE list shared by the engine (whitelist +
 * HELLO clamps in room-engine.ts) and the renderer (the picker + pills).
 */
export const CHAT_REACT_EMOJIS = ['👍', '❤️', '😂', '🔥', '😮', '🎉', '😢', '💀'] as const;
